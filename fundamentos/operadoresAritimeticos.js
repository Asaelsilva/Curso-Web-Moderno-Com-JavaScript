const [a, b, c, d] = [1, 2, 3 , 4]

const soma  = a + b + c + d
const subtracao = d - b
const multiplicacao = a * b
const divisao = d / a 
const modulo = a % 2

console.log(soma, subtracao, multiplicacao, -divisao, modulo)

//a++ operador postfix, fica depois do operando
//++a operador prefix, fica antes do operando
//-a unario
// a + b infix

